// Write your solution in this file!
var employees = {};

function updateEmployeeWithKeyAndValue(employee, key, value){
    for (const key in employee) {
        delete employee[key];
      }

      const name = Sam;
      const streetAddress = "11 Broadway";
}

function destructivelyUpdateEmployeeWithKeyAndValue(employee, key, value){
    employee(); {
        const name = Sam;
      const streetAddress = "11 Broadway";
      return employee;
    }
}

function deleteFromEmployeeByKey(employee, key){
    let newEmployee = deleteFromEmployeeByKey(employee, 'name');
    typeofnewEmployee = Sam;
    return newEmployee;
}

function deleteFromEmployeeByKey(employee, Sam);{
    employee = Sam;
    return employee;
}

function destructivelyDeleteFromEmployeeByKey(employee, key){
    let newEmployee = destructivelyDeleteFromEmployeeByKey(employee, Sam);
    employee == Sam;
    employee == newEmployee;
    return newEmployee;
}